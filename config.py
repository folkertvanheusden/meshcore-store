#radio_hostname='MCWS-65763c.local'  # replace this with the hostname or IP-address of the radio node
radio_hostname='192.168.65.88'  # replace this with the hostname or IP-address of the radio node

db_file='../retriever/data.db'  # database file to store the data in
